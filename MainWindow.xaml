<Window x:Class="BrawlhallaOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Brawlhalla Input Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#66000000" CornerRadius="10" Padding="10" BorderThickness="1" BorderBrush="#33FFFFFF">
        <StackPanel>
            <TextBlock x:Name="HintText"
                       Text="Ctrl+Alt+L : verrouiller / déverrouiller · glisser pour déplacer"
                       Foreground="#CCFFFFFF"
                       FontSize="10"
                       TextWrapping="Wrap"
                       Margin="2,0,2,6"
                       Visibility="Collapsed" />
            <StackPanel x:Name="MainRow" Orientation="Horizontal" />
        </StackPanel>
    </Border>
</Window>
